{
  "name": "event-server",
  "version": "0.1.0",
  "description": "Raspberry PI Event Server",
  "main": "app.js",
  "dependencies": {
    "debug": "^3.1.0",
    "dotenv": "^6.0.0",
    "express": "^4.16.3"
  },
  "scripts": {
    "start": "npm install && nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kairos DS",
  "license": "MIT"
}
